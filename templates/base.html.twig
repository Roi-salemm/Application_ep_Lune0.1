<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <style>
                body { background: #0a0a0a; color: #f5f5f5; font: 18px/1.5 sans-serif; margin: 0; }
                a { color: inherit; text-decoration: none; }
                .home-dark { min-height: 100vh; display: grid; place-items: center; background: radial-gradient(circle at top, #1a1a1a 0%, #0a0a0a 55%, #050505 100%); }
                .home-dark__content { text-align: center; padding: 2rem; max-width: 600px; }
                .home-dark__title { font-size: 32px; margin: 0 0 0.5rem; letter-spacing: 0.04em; text-transform: uppercase; }
                .home-dark__subtitle { margin: 0 0 2rem; color: #b5b5b5; }
                .home-dark__button { display: inline-block; padding: 0.9rem 1.6rem; border: 1px solid #f5f5f5; border-radius: 999px; font-weight: 600; transition: all 0.2s ease; }
                .home-dark__button:hover { background: #f5f5f5; color: #0a0a0a; }
            </style>
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
